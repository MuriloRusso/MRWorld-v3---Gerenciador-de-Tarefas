import InputNotes from "../../components/InputNotes";

export default function ModalPanelNotes({fieldsData, handleChange}:{fieldsData: any, handleChange: any}){
    return <InputNotes fieldsData={fieldsData} handleChange={handleChange}/>
}